{
  "name": "lifelike",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "restore": "dotnet restore && cd LifeLike.Web && npm install",
    "build": "webpack && dotnet build",
    "start": "webpack && dotnet run"
  }
}
